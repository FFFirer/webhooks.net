const BsPaginationProps = {
    page: {
        type: Number,
        default: 1,
    },
    pageSize: {
        type: Number,
        default: 20,
    },
    total: {
        type: Number,
        default: 0,
    },
};

export default BsPaginationProps;
